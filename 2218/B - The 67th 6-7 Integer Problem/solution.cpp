#include <bits/stdc++.h>using namespace std;typedef long long ll;ll MOD = 998244353;int main(){    ll t;    cin >> t;    while(t--){        vector<ll> a(7,0);        for(int i=0;i<7;i++){            cin >> a[i];        }        sort(a.begin(),a.end());        ll ans = 0;        for(int i=0;i<6;i++){            ans -= a[i];        }        ans += a[6];        cout << ans << endl;    }}