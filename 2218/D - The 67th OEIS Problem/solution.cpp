#include <bits/stdc++.h>using namespace std;typedef long long ll;ll MOD = 998244353; int main() {    ll mx = 200000;    vector<ll> p;    vector<bool> is_p(mx + 1, true);    is_p[0] = false;    is_p[1] = false;    for (ll i = 2; i <= mx; i++) {        if(is_p[i]) {            p.push_back(i);            for (ll j = 2 * i; j <= mx; j += i)is_p[j] = false;        }    }    ll t;    cin >> t;    while (t--) {        ll n;        cin >> n;        for (ll i = 0; i < n; i++) {            cout << (p[i] * 1LL * p[i + 1]) << " ";        }        cout << endl;    }    return 0;}